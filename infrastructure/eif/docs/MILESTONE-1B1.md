# Milestone 1B.1 Core Runtime

Load with `source "$EIF_ROOT/framework/core/framework.sh"`. Configuration precedence: defaults, environment, host, optional runtime override. Fail-closed defaults prohibit service restart, package removal, and firewall changes.
