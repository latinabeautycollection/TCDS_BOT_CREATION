import fs from 'node:fs/promises'; import path from 'node:path'; import { config } from './config.js'; import { log } from './log.js'; import { triggerDataset,waitForSnapshot,downloadSnapshot } from './brightdata.js'; import { AmazonRecordSchema } from './types.js'; import { platformAndConfig,createRun,finishRun,deadLetter,ingestRecord,saveEvidence,pool } from './db.js'; import { sha256,stableJson } from './util.js'; import { unlockAmazonPage } from './unlocker.js';
async function localDlq(payload:unknown,code:string,error:unknown){await fs.mkdir(config.DLQ_DIRECTORY,{recursive:true});await fs.writeFile(path.join(config.DLQ_DIRECTORY,`${Date.now()}-${code}-${sha256(stableJson(payload)).slice(0,12)}.json`),JSON.stringify({ts:new Date().toISOString(),code,error:String(error),payload},null,2));}
async function mapLimit<T>(items:T[],limit:number,fn:(x:T)=>Promise<void>){let i=0;await Promise.all(Array.from({length:Math.min(limit,items.length)},async()=>{while(true){const n=i++;if(n>=items.length)return;await fn(items[n]!);}}));}
async function main(){const {platformId,configId}=await platformAndConfig();const runKey=`amazon:${new Date().toISOString().slice(0,13)}:${sha256(config.keywords.join('|')+'|'+config.AMAZON_ZIPCODE).slice(0,12)}`;let runId='';const stats={collected:0,failed:0,skipped:0};try{runId=await createRun(platformId,configId,runKey,config.keywords);log('info','amazon_run_started',{runId,keywords:config.keywords,zipcode:config.AMAZON_ZIPCODE?'configured':'blank'});const snapshotId=await triggerDataset(config.keywords);log('info','dataset_triggered',{runId,snapshotId});await waitForSnapshot(snapshotId);const rows=await downloadSnapshot(snapshotId);log('info','snapshot_downloaded',{runId,snapshotId,rows:rows.length});await mapLimit(rows,config.INGEST_CONCURRENCY,async raw=>{const p=AmazonRecordSchema.safeParse(raw);if(!p.success){stats.failed++;await deadLetter(platformId,runId,raw,'VALIDATION_ERROR',p.error.message);await localDlq(raw,'VALIDATION_ERROR',p.error);return;}try{await ingestRecord(platformId,runId,p.data);stats.collected++;}catch(e){stats.failed++;await deadLetter(platformId,runId,p.data,'INGEST_ERROR',String(e));await localDlq(p.data,'INGEST_ERROR',e);if(config.AMAZON_UNLOCKER_MODE==='fallback'){try{const ev=await unlockAmazonPage(p.data.url);await saveEvidence(platformId,runId,p.data.url,ev.zone,ev.body,ev.contentType,ev.status,{phase:'fallback',ingest_error:String(e),page_not_found:ev.pageNotFound});}catch(ue){await deadLetter(platformId,runId,{url:p.data.url,original:p.data},'UNLOCKER_FALLBACK_ERROR',String(ue));}}}});if(rows.length===0)await deadLetter(platformId,runId,{keywords:config.keywords,zipcode:config.AMAZON_ZIPCODE},'EMPTY_SNAPSHOT','Amazon discovery snapshot returned zero records');await finishRun(runId,'completed',stats);log('info','amazon_run_completed',{runId,...stats});}catch(e){if(runId)await finishRun(runId,'failed',stats,String(e));log('error','amazon_run_failed',{runId,error:String(e),...stats});throw e;}finally{await pool.end();}}
main().catch(()=>process.exitCode=1);
